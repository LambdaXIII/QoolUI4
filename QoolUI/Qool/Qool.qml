pragma Singleton

import QtQuick

QtObject {

    enum AnchorPoints {
        TopLeft,
        Top,
        TopRight,
        Left,
        Center,
        Right,
        BottomLeft,
        Bottom,
        BottomRight
    }
}
