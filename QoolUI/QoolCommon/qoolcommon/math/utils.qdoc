/*!
    \namespace qoolui::math
    \inmodule QoolCommon
    \brief 纯函数数值工具集（无状态、无副作用）。

    提供数值比较、边界约束、精度控制、线性映射与区间循环等纯函数。
    全部为模板，支持整型与浮点混合使用。

    QoolCommon 是仅头文件库，QDoc 不扫描头文件注释——本文件是
    \c QoolCommon/qoolcommon/math/utils.hpp 的 sidecar，承载
    math 命名空间与其函数的 QDoc 文档。
*/

/*!
    \fn template <typename N> bool qoolui::math::is_equal(N a, N b, float epsilon)
    判断 \c a 与 \c b 在 \c epsilon 容差内是否相等。

    两值绝对值均小于 \c epsilon 时视为相等（避免相对误差除零）；
    否则按相对误差 \c |a-b|/max(|a|,|b|) < \c epsilon 判定。
*/

/*!
    \fn template <typename N> bool qoolui::math::is_zero(N a, float epsilon)
    判断 \c a 的绝对值是否小于 \c epsilon。
*/

/*!
    \fn template <typename N> N qoolui::math::auto_bound(N left, N x, N right)
    将 \c x 约束到 \c left 与 \c right 之间（端点乱序时自动取小大为界）。
*/

/*!
    \fn template <typename N, typename P> N qoolui::math::set_precision(N number, P precision)
    将 \c number 舍入到 \c precision 位小数；\c precision 为 0 时取整。
*/

/*!
    \fn template<typename T, typename U> U qoolui::math::remap(T input, T in_min, T in_max, U out_min, U out_max)
    将 \c input 从输入范围线性映射到输出范围（浮点中间计算，避免整数除法误差）。
*/

/*!
    \fn template <typename N> N qoolui::math::cycle_in_range(N min, N value, N max)
    将 \c value 循环约束到 \c min 与 \c max 之间（模数回绕，非钳制）。

    \section1 与钳制（auto_bound）的区别

    钳制把超界值钉在边界上（\c auto_bound(0, 12, 10) → 10）；本函数把
    区间视为环，超界值按模数折回（\c cycle_in_range(0, 12, 10) → 2）。
    从 \c max 一侧继续向外走会绕回 \c min，反之亦然。

    \section1 典型用途

    \list
      \li 角度归一化：任意角度折返到 [-180°, 180°]，如 \c cycle_in_range(-180, 540, 180) → -180
      \li 循环索引：末位 +1 绕回开头（12 点制时钟 12 时 +1 → 1 时）
    \endlist

    \section1 算法

    \list 1
      \li 端点排序得 \c left = min(min,max) / \c right = max(min,max)
      \li \c value 落在 [\c left, \c right] 内（含两端点）原样返回
      \li 区间外对 \c range = \c right - \c left 取模折返：\c fmod(value - left, range)，
          负余数加 \c range 修正，结果落在 [\c left, \c right)
    \endlist

    端点乱序（\c min > \c max）时自动取小大为界，语义与 \c auto_bound 一致。

    示例（min=0, max=10）：5 → 5；12 → 2；-3 → 7；10 → 10。

    \note \c N 应为有符号整型或浮点类型：无符号类型下区间外取值与
    负余数修正路径依赖 \c fmod 的负返回值，为未定义行为。
*/

/*!
    \fn template<typename N> N qoolui::math::average(std::initializer_list<N> numbers)
    返回初值列表的算术平均值。
*/
