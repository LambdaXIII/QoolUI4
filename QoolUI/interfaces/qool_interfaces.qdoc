/*!
    \page qool-plugin-interfaces.html
    \brief 插件接口：接口契约与插件约定。

    interfaces/ 目录承载 QoolUI 的插件接口头文件。接口是 QoolUI
    对外提供的插件契约面（ThemeLoader、FileIconProvider 等），
    与库本体物理分离：官方插件仅是参考实现，逻辑与物理上皆可选；
    第三方插件按接口自行实现，跟随版本演进（宽松承诺）。

    \section1 插件约定

    所有官方插件遵循以下约定，第三方插件建议同样遵守。

    \section2 元数据字段

    插件通过 \c Q_PLUGIN_METADATA 携带 json 元数据文件。约定字段：

    \list
    \li \c name —— 插件名称（\c PluginLoader 按名称登记）。
    \li \c author —— 作者标识。
    \li \c priority —— 插件优先级（见下节）。
    \endlist

    \section2 优先级（priority）

    插件优先级**统一在插件 json 元数据的 \c priority 字段定义**，
    由 \c PluginLoader 从插件元数据读取（\c pluginMetadata），
    接口不提供 priority 方法。

    **所有官方插件 json 必须包含 \c priority 字段，即使接口不需要**
    ——这是 v4 的约定性规范，非可选。优先级用于多插件并存时的
    覆盖/排序裁决（如多个颜色名提供插件按优先级决定查询顺序）。

    \section2 接口头文件

    接口头文件（\c interfaces/*.h）仅包含接口声明与简单注释，
    不重复本页的约定叙述；QDoc 注释不写入发布头。
*/
