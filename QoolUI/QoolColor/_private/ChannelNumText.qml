import QtQuick
import Qool

Text {
    id: root

    font: PixelFont.normal
    color: Style.text
    horizontalAlignment: Text.AlignRight
    verticalAlignment: Text.AlignVCenter

    visible: opacity > 0
}
