# CONTEXT — QoolUI4

本文件记录一些概念、沟通方式的约定。项目决策记录见 `docs/adr/`。

**术语**是和用户沟通时约定的一些「说法」（用语）。
在本项目范围（或具体领域范围）内，[SHOULD]使用这些术语特指其特定的概念；
目的在于消除歧义、对齐概念，保证 on a same page.

这些术语包括，但不限于: 
- 常见普通词汇的特指或重定义
- 复杂概念的简称

术语不是:
- 已有的技术用于
- 常用的概念、说法、专有名词
- 特定组件的名称、方法或属性的名字
- 做法约定、行为契约

[MUST] 拟对本文档的更改，必须先和用户对齐方案再执行；[MUST NOT] 不得随意修改

## 仓库级

**仓库**/**项目**/**根**: 特指本仓库，包含三个板块。

**板块**: 特指本仓库中 QoolUI / QoolUIExample / QoolUITests 三大板块

**模块**: 特指 QoolUI 板块中的各个 QML 包，如 Qool、Qool.Controls 等

**编译设施**: 本仓库的编译脚本和cmake设置等配置

**测试设施**: 特指 QoolUITests 项目（的架构、做法等一套规范）

**属性**: 特指 Qt 元对象系统中的 property

## 技术用语

**插件**: 通常指QtPlugin

**插拔接口**: 组件中特意设计的可替换子元素的接入点

**插拔件**: 插拔接口上安装的对象

**全局单例**/**进程级单例**/**APP级单例**: 通过C++实现的单例，进程中唯一实例

**QML单例**/**引擎级单例**: 在QML中使用的单例，由QML引擎管理，仅保证每个引擎中唯一对象，多引擎之间独立

---
应当[SHOULD]充分并谨慎地使用这些术语来表达对应概念，避免歧义
